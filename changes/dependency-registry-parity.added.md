---
"githits": minor
"@githits/mcp": minor
---

- **Expand dependency registry support** - `pkg_deps` now accepts NuGet, Maven, and Packagist alongside the other deployed dependency registries.
  <!-- Release preparation must update `skills/githits-package/SKILL.md` only after the backing behavior is in the release branch. -->
