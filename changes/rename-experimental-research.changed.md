---
"githits": minor
"@githits/mcp": none
---

- **Rename experimental research entrypoints** - The local MCP tool is now `research`; the CLI uses `githits research` and keeps `githits ask` as an alias. Both CLI spellings now emit `command.research` telemetry instead of `command.ask`.
