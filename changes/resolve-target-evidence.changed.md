---
"githits": patch
"@githits/mcp": none
---

- **Explain target-resolution evidence** - `githits resolve` and its local
  experimental MCP counterpart expose backend name similarity, render it as
  coarse lexical support without reranking, and distinguish indexed package and
  repository snapshots from exact commit-backed code readiness. Experimental
  JSON candidates no longer project backend ranking `reason` text.
