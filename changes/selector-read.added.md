---
"githits": minor
"@githits/mcp": minor
---

- **Selector reads** - Add `--selector` to `githits read` and `selector` to MCP `read` for documentation headings and indexed code symbols, including typed ambiguity, miss, and unsupported-snapshot outcomes.
