---
"githits": minor
"@githits/mcp": none
---

- **Experimental CLI defaults** - Hide and disable the released `resolve` and `code diff` commands by default; enable them explicitly with `[experimental] tools = true` while keeping the public MCP package unchanged.
