---
"githits": minor
"@githits/mcp": minor
---

- **Compact code-symbol reads** - Read package and repository symbols with `target#symbol` in the CLI and MCP tool, with an optional exact path; HTTP(S) documentation fragments and repository documentation page IDs keep their documentation behavior. Hosted MCP support requires a package release and adoption by the remote server.
